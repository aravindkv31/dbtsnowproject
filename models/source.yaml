version: 2

sources:
  - name: raw
    database: MYFIRSTPROJ
    schema: PUBLIC
    tables:
      - name: STOCK_PRICES
        description: "Raw data loaded from API"
        columns:
          - name: open
            tests:
              - not_null
